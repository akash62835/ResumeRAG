const {
  generateEmbedding,
  cosineSimilarity,
  chunkText,
  extractResumeData,
  analyzeJobMatch,
} = require("./gemini");

module.exports = {
  generateEmbedding,
  cosineSimilarity,
  chunkText,
  extractResumeData,
  analyzeJobMatch,
};
